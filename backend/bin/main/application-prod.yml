spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa.show-sql: false
  sql.init.mode: never

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    com.coliving: INFO
